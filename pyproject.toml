[project]
name = "correction-potential-stopping"
version = "0.1.0"
description = "基于修正潜力保护、无需答案探针的句子级推理停止方法"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
target-version = "py310"
line-length = 100
